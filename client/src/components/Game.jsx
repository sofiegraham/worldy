import React from 'react';

const Game = (props) => {
  return (
    <div>
      <div>
        {/* show a random flag and four random country names 
        props.country.flag
        props.fault
        
        */}
        <img src="https://restcountries.eu/data/zmb.svg"/>
      </div>

      
    </div>
  );
};

export default Game;